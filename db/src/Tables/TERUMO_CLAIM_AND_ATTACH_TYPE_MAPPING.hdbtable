COLUMN TABLE "TERUMODRMS_DB.db.Tables::TERUMO_CLAIM_AND_ATTACH_TYPE_MAPPING"(
	CLAIM_CODE INTEGER NOT NULL,
	ATTACH_CODE INTEGER NOT NULL,
	ATTACH_NO INTEGER NOT NULL,
	ATTACH_NAME NVARCHAR(100),
	LAST_UPDATED TIMESTAMP,
	CREATED_ON TIMESTAMP,
	PRIMARY KEY(CLAIM_CODE, ATTACH_CODE, ATTACH_NO)
)