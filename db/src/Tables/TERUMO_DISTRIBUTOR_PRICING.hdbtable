COLUMN TABLE "TERUMODRMS_DB.db.Tables::TERUMO_DISTRIBUTOR_PRICING"(
	OB_NO BIGINT NOT NULL,
	-- DISTRIBUTOR_NO NVARCHAR(10) NOT NULL,
	MATERIAL_CODE NVARCHAR(40) NOT NULL,
	SALES_ORG NVARCHAR(4),
	DIST_CHANNEL NVARCHAR(2),
	PRICELIST_TYPE INTEGER  NOT NULL,
	VALID_FROM DATE,
	VALID_TO DATE,
	REF_AMOUNT DOUBLE,
	AMOUNT DOUBLE,
	CURRENCY NVARCHAR(5),
	PER_UNIT DOUBLE,
	UOM NVARCHAR(3),
	PRIMARY KEY (OB_NO, MATERIAL_CODE, PRICELIST_TYPE)
)